enum ToastMessageType {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
}

enum FormTypeEnums {
  INPUT = 'input',
  SELECT = 'select',
  SWITCH = 'switch',
  TEXT = 'text',
}

export { ToastMessageType, FormTypeEnums };
