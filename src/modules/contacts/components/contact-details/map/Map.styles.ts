import { styled } from 'styled-components';

export const MapContainer = styled.section`
  margin-top: 12px;
  display: flex;
  align-items: center;
  justify-content: center;

  img {
    width: 100%;
    max-width: 330px;
    aspect-ratio: 1 / 1;
  }
`;
